class Solution {
    static List<Integer> get(int a, int b) {
        // code here
        List<Integer> res = new ArrayList<>();
        res.add(b);
        res.add(a);
        
        return res;
    }
}